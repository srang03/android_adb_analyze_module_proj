### **Android 로그 기반 카메라 탐지 연구 - 종합 실험 가이드 (v3.0)**

---

## **📋 1. 연구 개요**

### **1.1. 연구 목적**
- **핵심 목표**: 선별된 **6종 Android 시스템 로그**의 **개별적인 카메라 탐지 성능** 정량적 측정
- **혁신적 접근**: **이중 분석 프레임워크**를 통한 "카메라 사용" vs "카메라 촬영" 탐지 성능 분리 측정
- **실무적 기여**: "언제 어떤 로그를 우선 확인해야 하는가"에 대한 과학적 가이드라인 제공

### **1.2. 전체 실험 구조**
```
📊 총 192세션 = 파일럿 32세션 + 본실험 160세션

파일럿 테스트 (32세션):
- 목적: 이중 분석 프레임워크 검증 & 로그별 실행가능성 확인
- 구조: 4앱 × 2케이스 × 4휘발성 × 1반복 = 32세션
- 역할: 기술적 검증 중심, 통계 분석에서는 제외

본 실험 (160세션):
- 목적: 가설 검증을 위한 대규모 데이터 수집
- 구조: 4앱 × 2케이스 × 4휘발성 × 5반복 = 160세션
- 역할: 통계적 가설 검증의 주 데이터
```

### **1.3. 이중 분석 프레임워크**
**동일한 세션 데이터 → 서로 다른 두 Ground Truth 비교**

```python
# 통계 분석용 Ground Truth (본실험 160세션만)
# 분석 1: 카메라 사용 탐지 성능
GT_usage = [1] * 160  # 본실험 160세션에서 카메라 앱 실행

# 분석 2: 카메라 촬영 탐지 성능  
GT_capture = [0]*80 + [1]*80  # 본실험: 촬영N=0(80개), 촬영Y=1(80개)

# 파일럿 테스트 (32세션)는 별도 질적 분석으로 활용
```

---

## **🔬 2. 파일럿 테스트 설계**

### **2.1. 목적 및 범위**
1. **이중 분석 프레임워크** 실행 가능성 검증
2. **6개 선정 로그**의 실제 탐지 성능 특성 파악
3. **4가지 휘발성 조건**에서의 로그 보존 특성 예비 확인
4. **효율적 실험 전략**의 타당성 검증

### **2.2. 실험 설계**
```
📐 파일럿 실험 구조:
- 앱 아키텍처: S1, S2, S3, S4 (4개)
- 행위 케이스: 촬영N, 촬영Y (2개)
- 휘발성 조건: V0, V-Time, V-Usage, V-Event (4개)
- 반복 횟수: 1회 (실행가능성 검증 목적)
- 총 세션: 4 × 2 × 4 × 1 = 32세션

파일럿 1회 수행의 학술적 근거:
- 목적 차별성: 기술적 검증 vs 성능 측정
- 이론적 선별 완료: 3.3절에서 이미 6개 로그 선별됨
- 통계적 추론 불필요: 탐지 가능/불가능 확인이 목적
```

### **2.3. 효율적 실험 전략**

**전략 1: V0-V-Time 연계 실험**
```
1. V0 조건 실험 수행
2. 동일 세션에 대해 24시간 후 V-Time 조건 자동 적용
3. 결과: 동일한 카메라 행위에 대한 시간 경과 효과 직접 비교
```

**전략 2: V-Usage 연속성 실험**
```
1. 첫 번째 카메라 행위 수행
2. 체계적 간섭 시나리오 수행:
   - 1단계: 브라우저 검색, 갤러리 확인, 동영상 재생, SNS 메시지
   - 2단계: 추가 카메라 세션 (4개 앱에서 각각 카메라 실행→촬영→종료)
3. 결과: 하나의 실험에서 2개 세션 데이터 동시 확보
```

---

## **🎯 3. 본 실험 설계**

### **3.1. 목적 및 범위**
- **가설 검증**: H1(앱 아키텍처 영향), H2(휘발성 영향)
- **대규모 데이터**: 160세션의 신뢰성 있는 성능 데이터
- **통계적 검정력**: G*Power 분석 결과 약 0.85 확보 (목표 0.80 초과)
- **표본크기**: 그룹당 40개 (G*Power 요구 36개의 111% 달성)

### **3.2. 마스터 데이터시트 구조**

| Session ID | App | Case | Volatility | Rep | GT_Usage | GT_Capture | Log_Path | Hash | 사용_Pred | 촬영_Pred | 사용_Result | 촬영_Result | Notes |
|:-----------|:----|:-----|:-----------|:----|:---------|:-----------|:---------|:-----|:----------|:----------|:------------|:------------|:------|
| ME-S1-N-V0-R1 | S1 | 촬영N | V0 | 1 | 1 | 0 | ./logs/... | SHA256 | 분석시 | 분석시 | TP/FP/TN/FN | TP/FP/TN/FN | 오류시만 |

**새로운 컬럼 설명**:
- **사용_Pred**: 카메라 사용 탐지 예측 결과 (0/1)
- **촬영_Pred**: 카메라 촬영 탐지 예측 결과 (0/1)  
- **사용_Result**: GT_Usage vs 사용_Pred 비교 결과
- **촬영_Result**: GT_Capture vs 촬영_Pred 비교 결과

---

## **📊 4. 데이터 분석 방법론**

### **4.1. 개별 로그 분석 함수**
```python
def analyze_individual_log(session_data, log_type):
    """각 로그의 개별 성능 측정"""
    
    if log_type == 'media.camera':
        patterns = ['CONNECT', 'DISCONNECT', 'Camera2ClientBase']
        return detect_service_events(session_data, patterns)
        
    elif log_type == 'usagestats':
        return detect_usage_changes(session_data)
        
    elif log_type == 'activity':
        patterns = ['START', 'RESUME', 'MediaStore.ACTION_IMAGE_CAPTURE']
        return detect_activity_events(session_data, patterns)
        
    elif log_type == 'audio':
        patterns = ['AudioFocus', 'STREAM_NOTIFICATION', 'camera_click.ogg']
        return detect_audio_events(session_data, patterns)
        
    elif log_type == 'media.metrics':
        return detect_media_processing(session_data)
        
    elif log_type == 'vibrator_manager':
        patterns = ['vibrate', 'VibratorManager', 'haptic_feedback']
        return detect_haptic_events(session_data, patterns)

def dual_analysis_framework(session_data, gt_usage, gt_capture):
    """이중 분석 프레임워크 적용 (본실험 160세션 기준)"""
    results = {}
    
    for log_type in ['media.camera', 'usagestats', 'activity', 
                     'audio', 'media.metrics', 'vibrator_manager']:
        
        # 동일한 예측값으로 두 가지 분석 수행
        prediction = analyze_individual_log(session_data, log_type)
        
        # 분석 1: 카메라 사용 탐지 성능 (본실험 160세션)
        usage_performance = calculate_performance(gt_usage, prediction)
        
        # 분석 2: 카메라 촬영 탐지 성능 (본실험 160세션) 
        capture_performance = calculate_performance(gt_capture, prediction)
        
        results[log_type] = {
            'usage_performance': usage_performance,
            'capture_performance': capture_performance
        }
    
    return results

# 사용 예시
GT_usage_main = [1] * 160    # 본실험 160세션 모두 카메라 사용
GT_capture_main = [0]*80 + [1]*80  # 본실험 촬영N 80개, 촬영Y 80개
results = dual_analysis_framework(main_experiment_data, GT_usage_main, GT_capture_main)
```

### **4.2. 개별 로그별 독립적 통계 분석 (본실험 160세션 기준)**
```python
# 본실험 160세션 기반 통계 분석 (각 그룹당 40개 표본)
# 총 24개 독립적 ANOVA 분석 (6로그 × 2분석 × 2가설)

for log in ['media.camera', 'usagestats', 'activity', 'audio', 'media.metrics', 'vibrator_manager']:
    
    # === H1 검증: 앱 아키텍처 영향 (각 그룹당 40개: 2케이스 × 4휘발성 × 5반복) ===
    S1_usage_data = get_log_performance(log, app='S1', analysis='usage')    # 40개
    S2_usage_data = get_log_performance(log, app='S2', analysis='usage')    # 40개  
    S3_usage_data = get_log_performance(log, app='S3', analysis='usage')    # 40개
    S4_usage_data = get_log_performance(log, app='S4', analysis='usage')    # 40개
    
    # H1-Usage 분석 (ANOVA #1-6)
    h1_usage_anova = scipy.stats.f_oneway(S1_usage_data, S2_usage_data, 
                                          S3_usage_data, S4_usage_data)
    
    # H1-Capture 분석 (ANOVA #7-12)
    S1_capture_data = get_log_performance(log, app='S1', analysis='capture') # 40개
    S2_capture_data = get_log_performance(log, app='S2', analysis='capture') # 40개  
    S3_capture_data = get_log_performance(log, app='S3', analysis='capture') # 40개
    S4_capture_data = get_log_performance(log, app='S4', analysis='capture') # 40개
    
    h1_capture_anova = scipy.stats.f_oneway(S1_capture_data, S2_capture_data, 
                                            S3_capture_data, S4_capture_data)
    
    # === H2 검증: 휘발성 영향 (각 그룹당 40개: 4앱 × 2케이스 × 5반복) ===
    
    # H2-Usage 분석 (ANOVA #13-18)
    V0_usage_data = get_log_performance(log, volatility='V0', analysis='usage')        # 40개
    VTime_usage_data = get_log_performance(log, volatility='V-Time', analysis='usage') # 40개
    VUsage_usage_data = get_log_performance(log, volatility='V-Usage', analysis='usage') # 40개
    VEvent_usage_data = get_log_performance(log, volatility='V-Event', analysis='usage') # 40개
    
    h2_usage_anova = scipy.stats.f_oneway(V0_usage_data, VTime_usage_data, 
                                          VUsage_usage_data, VEvent_usage_data)
    
    # H2-Capture 분석 (ANOVA #19-24)
    V0_capture_data = get_log_performance(log, volatility='V0', analysis='capture')        # 40개
    VTime_capture_data = get_log_performance(log, volatility='V-Time', analysis='capture') # 40개
    VUsage_capture_data = get_log_performance(log, volatility='V-Usage', analysis='capture') # 40개
    VEvent_capture_data = get_log_performance(log, volatility='V-Event', analysis='capture') # 40개
    
    h2_capture_anova = scipy.stats.f_oneway(V0_capture_data, VTime_capture_data, 
                                            VUsage_capture_data, VEvent_capture_data)

# 2단계: 유의미한 결과에 대한 사후분석 (총 24개 ANOVA 중 p < 0.05인 경우만)
significant_results = []

# 각 ANOVA 결과에 대해 사후분석 수행
for log, analysis_type, hypothesis in significant_anova_results:
    if hypothesis == 'H1':  # 앱 아키텍처 영향
        # Tukey HSD (앱 간 다중비교)
        tukey_result = scipy.stats.tukey_hsd(S1_data, S2_data, S3_data, S4_data)
        significant_results.append((log, analysis_type, 'H1', tukey_result))
        
    elif hypothesis == 'H2':  # 휘발성 영향
        # Dunnett Test (V0을 기준으로 한 비교)
        dunnett_result = compare_with_control(V0_data, [VTime_data, VUsage_data, VEvent_data])
        significant_results.append((log, analysis_type, 'H2', dunnett_result))

# 파일럿 테스트 32세션은 질적 분석으로 별도 처리
pilot_results = analyze_pilot_feasibility(pilot_32_sessions)

print(f"총 24개 ANOVA 중 {len(significant_results)}개 유의미한 결과 확인")
```

---

## **🔧 5. 실험 수행 절차**

### **5.1. 세션 실행 프로토콜**
```bash
# 1. 기기 초기화
adb shell am force-stop-all
adb shell pm clear [앱패키지]

# 2. 표준 행위 수행
# 촬영N: 카메라 앱 실행 → UI 진입 10초 → 종료 (촬영 안함)
# 촬영Y: 카메라 앱 실행 → UI 진입 → 촬영 1회 → 종료

# 3. 휘발성 조건 적용
case $VOLATILITY in
    "V0") echo "즉시 진행" ;;
    "V-Time") echo "24시간 대기" ;;
    "V-Usage") ./interference_scenario.sh ;;
    "V-Event") adb reboot ;;
esac

# 4. 로그 수집
./collect_6logs.sh $SESSION_ID
```

### **5.2. 데이터 무결성 보장**
```bash
# SHA-256 해시값 자동 계산
for session in session_list; do
    tar -czf ${session}.tar.gz ./logs/${session}/
    hash=$(sha256sum ${session}.tar.gz | cut -d' ' -f1)
    echo "${session},${hash}" >> integrity.log
done
```

---

## **📈 6. 예상 결과**

### **6.1. 로그별 특화 영역**
```
🔹 카메라 사용 탐지 특화:
- media.camera: 서비스 활성화 직접 탐지
- usagestats: 앱 사용 패턴 변화
- activity: Activity 전환 패턴

🔹 카메라 촬영 탐지 특화:
- audio: 셔터음 및 오디오 변화
- media.metrics: 미디어 처리 통계
- vibrator_manager: 햅틱 피드백
```

### **6.2. 실무 가이드라인**
```
목표 결과 예시:
"무음카메라(S4) 촬영 탐지 시:
 1순위: audio 로그 (F1=0.95, V-Event 제외)
 2순위: media.metrics (F1=0.87, 모든 조건)
 주의: 재부팅 후에는 제한적"
```

---

## **✅ 7. 성공 기준**

### **7.1. 정량적 기준**
- [ ] 192세션 완전 수집 (파일럿 32 + 본실험 160)
- [ ] 데이터 무결성 100% 검증
- [ ] 통계적 검정력 약 0.85 확보 (목표 0.80 초과)
- [ ] 6개 로그 이중 분석 완료
- [ ] 본실험 160세션 기반 가설 검증 완료

### **7.2. 최종 산출물**
1. 완성된 마스터 데이터시트 (192행: 파일럿 32 + 본실험 160)
2. 로그별 이중 성능 분석 결과 (본실험 160세션 기준)
3. 통계적 가설 검증 리포트 (그룹당 40개 표본)
4. 실무 활용 가이드라인
5. 데이터 무결성 증명서
6. 파일럿 테스트 질적 분석 보고서

---

**이 가이드는 192세션(파일럿 32 + 본실험 160)의 이중 분석 기반 연구를 체계적이고 재현가능하게 수행할 수 있도록 설계되었습니다.**
